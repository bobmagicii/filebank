# filebank
Watches a directory of files and after a file gets read, replaces that file with a random one from the filebank. This way it is different every time it is read. Intended mostly for pre-rendering voice wav files with 200 different ways to say "job's done" and all the apps that can play sounds will have variation.
